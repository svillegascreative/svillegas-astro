---
import { getEntry } from "astro:content";
import Page from "../layouts/Page.astro";

const post = await getEntry("pages", "colophon");

const { Content } = await post.render();
---

<Page title="colophon" lede={post.data.lede}>
  <Content />
</Page>
